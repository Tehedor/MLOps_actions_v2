# ============================================================
# MLOps4OFP — Workflow Configuration
# ============================================================

# ------------------------------------------------------------
# GIT — Checkout configuration
# ============================================================
git:
  checkout_branch: "test2"  # Rama por defecto para GitHub Actions checkouts
